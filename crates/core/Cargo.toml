[package]
name = "chauffeur-core"
description = "Chauffeur SDK: the Sense → Classify → Act engine, System One interface, capability contracts, and daemon client"
version.workspace = true
edition.workspace = true
license.workspace = true
rust-version.workspace = true
repository.workspace = true

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
reqwest = { workspace = true, optional = true }

[features]
default = ["client"]
# The HTTP daemon client; off for the wasm engine build.
client = ["dep:reqwest"]

[lints]
workspace = true
